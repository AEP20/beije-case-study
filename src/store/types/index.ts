export type PedState = {
  standard: number;
  super: number;
  superPlus: number;
};

export type DailyPedState = {
  daily: number;
  superDaily: number;
};

export type TamponState = {
  mini: number;
  standard: number;
  super: number;
};
